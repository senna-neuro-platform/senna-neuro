global:
  scrape_interval: 5s
  evaluation_interval: 5s
  scrape_timeout: 4s

scrape_configs:
  - job_name: "senna-core"
    metrics_path: /metrics
    static_configs:
      - targets: ["senna-core:9090"]
        labels:
          service: "senna-core"
